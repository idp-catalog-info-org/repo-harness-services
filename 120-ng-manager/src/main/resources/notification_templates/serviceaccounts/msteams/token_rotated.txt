{
  "@type": "MessageCard",
  "themeColor": "#FF8C00",
  "summary": "Token rotated — ${TOKEN_IDENTIFIER}",
  "sections": [
    {
      "activityTitle": "The token ${TOKEN_IDENTIFIER} for API Key ${API_KEY_IDENTIFIER} in Service Account ${SERVICE_ACCOUNT_NAME} was rotated.",
      "activityText": "The old token will remain valid until ${EXPIRY_DATE}. Update any pipelines or integrations to use the new token.",
      "facts": [
        {
          "name": "Old Token Valid Until",
          "value": "${EXPIRY_DATE}"
        },
        {
          "name": "Token Identifier",
          "value": "${TOKEN_IDENTIFIER}"
        },
        {
          "name": "API Key Identifier",
          "value": "${API_KEY_IDENTIFIER}"
        },
        {
          "name": "Service Account",
          "value": "${SERVICE_ACCOUNT_NAME}"
        },
        {
          "name": "Account Name",
          "value": "${ACCOUNT_NAME}"
        },
        {
          "name": "Account ID",
          "value": "${ACCOUNT_IDENTIFIER}"
        },
        {
          "name": "Organization",
          "value": "${ORG_IDENTIFIER}"
        },
        {
          "name": "Project",
          "value": "${PROJECT_IDENTIFIER}"
        }
      ]
    }
  ]
}
