{
    "title": "Token deleted — ${TOKEN_IDENTIFIER}",
    "text": "The token ${TOKEN_IDENTIFIER} for API Key ${API_KEY_IDENTIFIER} in Service Account ${SERVICE_ACCOUNT_NAME} was deleted.\n\nAny pipelines or integrations using this token will fail.\n\nAccount: ${ACCOUNT_NAME} | Account ID: ${ACCOUNT_IDENTIFIER} | Org: ${ORG_IDENTIFIER} | Project: ${PROJECT_IDENTIFIER}",
    "tags": [
        "event_type:token_deleted",
        "token_identifier:${TOKEN_IDENTIFIER}",
        "api_key_identifier:${API_KEY_IDENTIFIER}",
        "service_account_name:${SERVICE_ACCOUNT_NAME}",
        "account_name:${ACCOUNT_NAME}",
        "account_identifier:${ACCOUNT_IDENTIFIER}",
        "org_identifier:${ORG_IDENTIFIER}",
        "project_identifier:${PROJECT_IDENTIFIER}"
    ],
    "aggregation_key": "${TOKEN_IDENTIFIER}",
    "priority": "normal",
    "alert_type": "error",
    "source_type_name": "harness_notifications"
}
