{
  "@type": "MessageCard",
  "themeColor": "#FFFFFF",
  "title": "Harness Artifact Registry Lifecycle Rule Execution: ${POLICY_NAME} - ${EXECUTION_STATUS}",
  "summary": "Harness Artifact Registry Lifecycle Rule Execution: ${POLICY_NAME} - ${EXECUTION_STATUS}",
  "sections": [
    {
      "title": "Lifecycle Rule Execution: ${POLICY_NAME} - ${EXECUTION_STATUS}",
      "text": "A lifecycle rule execution has completed with status: ${EXECUTION_STATUS}.<br><br>Lifecycle Rule: ${POLICY_NAME}<br>Execution Start Time: ${EXECUTION_START_TIME}<br>Execution End Time: ${EXECUTION_END_TIME}<br>Artifacts Protected (Protection Rule): ${ARTIFACTS_SKIPPED}<br>Artifacts Soft Deleted: ${ARTIFACTS_DELETED}<br>Registries Affected: ${REGISTRIES_AFFECTED}<br>Storage Reclaimed: ${STORAGE_RECLAIMED}<br><br>View Details: ${EXECUTION_URL}",
      "potentialAction": [
        {
            "@type": "OpenUri",
            "name": "View Execution Details",
            "targets": [
                {
                "os": "default",
                "uri": "${EXECUTION_URL}"
                }
            ]
        }
      ]
    }
  ]
}
