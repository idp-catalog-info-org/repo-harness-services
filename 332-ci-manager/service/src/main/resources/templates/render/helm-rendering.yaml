id: ID_PLACEHOLDER
name: ID_PLACEHOLDER
run:
  type: run
  container:
    image: harnessdev/helm-manifest:0.0.1
    connector: account.harnessImage
  env:
    PLUGIN_MANIFEST_PATH: ${{runtime.manifestPath}}
    PLUGIN_VALUES_PATH: ${{serviceOutput.manifests.overrides}}
