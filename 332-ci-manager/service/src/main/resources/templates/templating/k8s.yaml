name: ID_PLACEHOLDER
id: ID_PLACEHOLDER
run:
  type: run
  container:
    image: harnessdev/k8s-template:0.0.1
    connector: account.harnessImage
  env:
    PLUGIN_MANIFEST_PATH: ${{serviceOutput.manifests.primary.paths}}
    PLUGIN_VALUES_PATH: ${{serviceOutput.manifests.overrides}}