name: Plan Concurrency Gate Metrics
identifier: PlanConcurrencyGateMetrics
metricGroup: PlanConcurrencyGateMetricsGroup
metrics:
  - metricName: pipeline_plan_concurrency_gate_decision_total
    metricDefinition: Counter-based plan concurrency gate decisions. decision in {queue, allow}; reason in {project, account, none}; mode in {enforce, shadow, disabled}.
    type: Count
    unit: "1"
